-- Assign the target 
	targetObj =  GetGUIDObject(targetGUID);

		--???

-- get self player with local obj as variable

	local localObj =	GetLocalPlayer();

		-- Assign self player as localObj variable


-- Get target object position 

	local targetObj:	GetPosition();

		-- Get the position variable T of variable targetObj 

				local xT, yT, zT = targetObj:GetPosition();


-- Get self player position 

	local localObj:	GetPosition();

		-- Get the position variable T of variable targetObj 

				local xT, yT, zT = localObj:GetPosition();

-- Gets the distance of current target and sets it as a variable

 	local distance = targetObj:GetDistance();

-- what is return 2 ??
	if(targetObj == 0 or targetObj == nil) then
		return 2;
	end

-- Get self player
	GetLocalPlayer();









	--			if (self.autoBlessing) then
--				if (not IsInCombat() and localMana > 40) then
--					if (partyMembersMana > 1) then
--						if (not partyMember:HasBuff("Blessing of Might")) then
--							return true;
--						end
--					end
--				end
--			end




--	for b = 1, GetNumPartyMembers()+1 do
--		local partyMemberA = GetPartyMember(b);
--		if (b == GetNumPartyMembers()+1) then
--			partyMemberA = GetLocalPlayer();
--		end
--	end